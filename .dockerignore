# 의존성 (빌드 시 새로 설치하므로 불필요)

node_modules

# 빌드 산출물 (Dockerfile 내부에서 새로 빌드하므로 불필요)

dist

# 환경변수 (보안상 이미지에 포함되면 안 됨)

.env
.env.\*
!.env.example

# 테스트/커버리지 (운영에 불필요)

coverage
test

# 사용자 업로드 파일 (볼륨으로 관리해야 함)

uploads

# Git 관련

.git
.github
.gitignore

# 에디터/설정

.eslintrc.js
.prettierrc
README.md
